'use strict';

/**
 * config-parameter-chield router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::config-parameter-chield.config-parameter-chield');
